export interface Experience {
  job: string;
  company: string;
  companyLogo: string;
  location: string;
  workType: string;
  start: string;
  end?: string;
  url?: string;
  description: string;
}
